# 敏感配置（包含 API 密钥）
.env

# 运行时产物
__pycache__/
*.pyc
*.pyo

# Legacy（旧 FastAPI 栈产物已从仓库删除；旧 checkout 本地仍可能残留，继续忽略防误提交）
pipeline.db
uploads/*
.server_pass

# IDE
.idea/

# 临时文件
*.log

# Claude Code 本地配置
.claude/

# 本地虚拟环境 / pytest 缓存
.venv/
.pytest_cache/

# 前端（Vite）
node_modules/
dist/
.vite/
*.local

# backend 运行时数据库
backend/db.sqlite3
backend/db.sqlite3-journal

# 容器编排运行时数据（instances/<name>/ home + openclaw.json，spec §5）
fleet/
