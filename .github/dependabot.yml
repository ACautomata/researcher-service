# Dependabot —— 让钉到完整 SHA 的 GitHub Actions 随上游发布自动提 PR 更新（供应链安全审查后续）。
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"            # 扫 .github/workflows/
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
